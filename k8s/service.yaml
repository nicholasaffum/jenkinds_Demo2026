apiVersion: v1
kind: Service

metadata:
  name: nick-jenkins-service

spec:
  type: NodePort

  selector:
    app: nick-jenkins

  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080
